#!/usr/bin/env sh
# . "$(dirname -- "$0")/_/husky.sh"

# Comment: This hook validates commit messages
# It ensures commit messages follow conventional commit format
# To bypass in emergency: git commit --no-verify

# npx --no -- commitlint --edit $1
